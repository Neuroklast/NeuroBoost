// Algorithms.cpp – compiled translation unit (currently empty;
// all algorithm logic lives in the header for inlining).
// A .cpp is provided so CMake can include it in the build without warnings.

#include "Algorithms.h"
